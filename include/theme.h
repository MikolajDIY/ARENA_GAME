#pragma once
#include <SFML/Graphics.hpp>

// Kolory dla Interfejsu
namespace Theme {
    // Przyciski Menu
    const sf::Color ButtonNormal(30, 30, 30, 220);  // Grafit
    const sf::Color ButtonHover(45, 80, 45, 240);   // Toksyczna zieleñ
    const sf::Color ButtonOutline(100, 70, 50);     // Rdza

    // Kolory Textu
    const sf::Color Text(230, 220, 200);            // Pergaminowy napis

    // Kolory
    const sf::Color Grey(70,70,70, 230);              // Szary
}
